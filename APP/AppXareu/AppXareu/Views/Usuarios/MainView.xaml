<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"  
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
            x:Class="AppXareu.Views.Usuarios.MainView"  
            Title="MainView">  
  
   <VerticalStackLayout>
        <Label  
           Text="{Binding Login}" /> <!-- esse Login não mostra e eu não consegui enterder o porque -->
        <Button Text="Editar" Command="{Binding VaiParaEditCommand}" Margin="0,10,0,0"/>
        <Button Text="Deletar" Command="{Binding DeletarCommand}" Margin="0,10,0,0"/>
    </VerticalStackLayout>  
</ContentPage>